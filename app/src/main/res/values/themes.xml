<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CRMMobile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/blue_gray</item>
    </style>

    <style name="Theme.CRMMobile" parent="Base.Theme.CRMMobile" />

    <style name="NoRippleBottomNav" parent="@style/Widget.MaterialComponents.BottomNavigationView">
        <item name="itemRippleColor">@android:color/transparent</item>
        <item name="itemActiveIndicatorStyle">@style/NoIndicator</item>
    </style>

    <style name="NoIndicator">
        <item name="android:color">@android:color/transparent</item>
    </style>
    <style name="Theme.App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Nền toàn app -->
        <item name="android:colorBackground">@color/so_screen_bg</item>

        <!-- Surface cho card/sheet (giữ trắng, không bị ám màu) -->
        <item name="colorSurface">@color/white</item>
        <item name="colorSurfaceVariant">@color/so_screen_bg</item>


        <!-- Áp style mặc định cho MaterialCardView -->
        <item name="materialCardViewStyle">@style/Widget.App.Card</item>
    </style>
</resources>